import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#DD2D51',
    },
    secondary: {
      main: '#19857b',
    },
    background: {
      default: '#F4F4F3',
      paper: '#F4F4F3'
    },

  },
});

export default theme;